# Watch Party BRD — Adversarial Self-Review Gate

> ⚠️ **Historical snapshot (v2/v3, 2026-06-24).** This verdict predates the **2026-06-28 dev-sync** changes — **single capacity** (no `advertisedCapacity`), **POPCORN refund credited in-system** (no "CS re-credits manually"), and **LiveKit** as the streaming layer (not IVS). The live spec above reflects 6-28; this review is kept as the record of the v2/v3 gate. See ADR `decisions/2026-06-28-watch-party-bo-changes.md`.

**Reviewer:** fresh-context adversarial reviewer (no prior context; verified against written files only)
**Date:** 2026-06-24
**Files reviewed:** `_shared-contract.md`, `watch-party/_feature.md`, `watch-party/BO.md`, `watch-party/Frontend.md`

| Check | Verdict | Evidence |
|---|---|---|
| 1. Manifest coverage (zero MISSING; every INTENTIONALLY-OMITTED has a reason) | **PASS** | The "Reference baseline (Mobbin)" manifest (`_feature.md` lines 104–132) has 27 rows; every `Coverage` cell is `COVERED` or `IMPROVED`. Zero `MISSING`, zero `INTENTIONALLY-OMITTED`. Legend at line 134 asserts "Zero MISSING." Confirmed by inspection. |
| 2. Safety — 7 ‡ lenses each RESOLVED or NEEDS-POLICY-OWNER (no UNRESOLVED / silent default) | **PASS** | In "Resolved decisions" table (`_feature.md` lines 82–88): Auth ‡ RESOLVED, Money ‡ RESOLVED (the *mechanism*), Concurrency ‡ RESOLVED, Failure/idempotency ‡ RESOLVED, Abuse/self-dealing ‡ RESOLVED, Trust&safety/licensing ‡ RESOLVED, Audit ‡ RESOLVED. All 7 carry populated min-fields + contract field(s). The one explicitly deferred item — *revenue split* (line 89) — is correctly `NEEDS-POLICY-OWNER` with a named human ("Owner: Susan (Finance)") and a question. None `UNRESOLVED`. (Note: age-gating is folded into the Trust&safety ‡ lens rather than a standalone row — it is covered, gate includes `ageRating`.) |
| 3. Safety — each RESOLVED ‡ lens maps to a TC whose `Then` asserts the specific failure prevented | **PASS** | Auth→TC-A1 ("`403` and **no** `WatchParty`/`Order` data") + TC-A2 ("fan A requests fan B's order → `403`/empty") = real A-cannot-read-B assertion. Money→TC-M1 (`amountPopcorn==150` AND `balance==50`, charged once) + TC-M2 (price `12.5` → **rejected**). Concurrency→TC-C1 ("**exactly one** Order created, the other gets room full"). Idempotency→TC-I1 ("same Order returned, POPCORN debited **once**"). Abuse→TC-AB1 (ep4/other title "**not selectable** and a forced request `403`s"). Trust&safety→TC-TS1 (geoblocked join "**blocked**, no stream token issued"). Audit→TC-AU1 ("`AuditLog` row exists AND `Order.status=='refunded'`"). Every `Then` is a concrete failure assertion, not a happy path. |
| 4. Contract integrity (forward) — every field/enum used in surface files defined in contract | **PASS** | No invented fields found. All surface usages resolve: `WatchParty.{name,scheduledStartAt,ticketPricePopcorn,isFree,titleId,episodeId,status,capacity,advertisedCapacity,ticketsSold}`, `Order.{amountPopcorn,status,idempotencyKey,userId,kind,refId}`, `Wallet.popcornBalance`, `Title.{geoPolicy,ageRating}`, `Episode`, `User.displayName`, `HostGrant`, `ContentGrant`, `RoomOccupancy.occupancy`, `EmailNotification`, `AuditLog.{actorId,action,targetId,at}` — all defined verbatim in `_shared-contract.md` or `_feature.md#shared-data-contract`. TC-AU1's `AuditLog{actorId, action, targetId, at}` is a subset of the defined 8-field shape (not an invention). |
| 5. Contract integrity (reverse) — every ‡ resolution names contract field(s) that exist | **PASS** | Auth → `HostGrant`, `ContentGrant`, `Order.userId` (✓ all defined). Money → `WatchParty.ticketPricePopcorn`, `Order.amountPopcorn`, `Order.status` (✓). Concurrency → `WatchParty.capacity`, `WatchParty.ticketsSold` (✓). Idempotency → `Order.idempotencyKey` (✓, defined `_shared-contract.md` line 62). Abuse → `ContentGrant`, `HostGrant`, `WatchParty.status` (✓). Trust&safety → `Title.geoPolicy`, `Title.ageRating`, `Order.status`, `WatchParty.status` (✓). Audit → `AuditLog.*` (✓). Every named field exists. |
| 6. State machine totality — every `WatchParty.status` enum in ≥1 transition; illegal transitions explicit | **PASS** | Enum (line 41): `draft, scheduled, preshow, live, host_away, ended, cancelled`. Transitions (lines 48–53) cover all 7: draft→scheduled; scheduled→preshow; preshow→live; live→host_away; host_away→live; live→ended; host_away→ended; (draft\|scheduled\|preshow)→cancelled. No orphan enum value. Illegal transitions explicitly listed (line 54): `ended ✗→ live/preshow/scheduled · cancelled ✗→ any · live ✗→ scheduled · any ✗→ draft`. |
| 7. Oracle — every TC `Then` is observable (DB state / API status / DOM text) | **PASS** | All 12 TCs assert observable state: HTTP `403` (TC-A1/A2/AB1), DB field values (`Order.amountPopcorn==150`, `Wallet.popcornBalance==50`, `Order.status=='refunded'/'confirmed'`, `status=='host_away'/'live'`), row existence (`AuditLog` row exists; "exactly one Order created"), and specific DOM copy ("room full," "whole POPCORN only," "not available in your region"). No vague "user sees confirmation" oracles. |
| 8. Assumptions logged — defaulted "invisible" gaps listed under Assumptions | **PASS** | `_feature.md` "Assumptions" (lines 139–148) logs 8 defaults: POPCORN-integer currency, 1-live-party-per-host rate guard, chat-not-persisted, start-reminder email, advertised-cap-default-50%, UTC-store/local-display, manual CS re-credit on refund, App-version-out-of-scope. Each maps to a defaulted invisible gap and states it is human-overridable (line 140). |

## Overall verdict: **PASS**

The spec is not hollow. All 7 non-negotiable ‡ safety lenses are resolved (or correctly escalated, in the one rev-share case) and each resolved lens is backed by a test case whose `Then` asserts the specific failure it prevents — concurrency ("exactly one succeeds"), idempotency ("debited once"), auth ("A cannot read B / 403"). The data contract is internally consistent in both directions with no invented fields, the state machine is total with explicit illegal transitions, every test oracle is observable, and defaulted gaps are logged as overridable assumptions.

## Required fixes
None — clean pass.

## Reviewer notes (non-blocking observations, not gate failures)
1. **Age-gating is not a standalone ‡ row.** The 7th non-negotiable lens (Trust&safety / **age-gating**) is merged into the single "Trust&safety / licensing" row. Age-gating *is* present in the resolution (gate includes `Title.ageRating`), but TC-TS1 only exercises the **geoblock** path, not the **age-rating** path. Consider adding a dedicated age-gate test (e.g., under-age viewer on an `R`-rated title → join blocked) so the `ageRating` field has its own failure assertion. Not a gate failure because the lens is resolved and contract-backed.
2. **TC-AU1 lists a partial `AuditLog` shape** (`{actorId, action, targetId, at}`) — omits `targetType`, `before`, `after`. This is a subset of the defined entity, not an invented field, so check 4 passes; but the test could assert `before`/`after` are captured to fully prove the audit trail is reconstructable.
3. **`Order.userId` is named in the Auth resolution** and exists in the contract (`_shared-contract.md` line 60) — confirmed, no issue.
